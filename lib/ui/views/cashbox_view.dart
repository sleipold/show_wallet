import 'package:flutter/material.dart';

class CashboxView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('CashboxView', style: TextStyle(fontSize: 36.0),),
    );
  }
}
